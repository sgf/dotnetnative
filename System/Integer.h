#pragma once
#include "pch.h"
#include "String.h"

namespace System {

	class Integer {

	public:
		static String& ToString(int32_t value, String& format) {

			if (value >= 0 && format.Length == 0)
			{
				//return UInt32ToDecStr((uint)value, digits: -1);
			}


		}



	};


}